package LeetCode

import scala.collection.mutable.Stack

object ValidParentheses {
  def isValid(s: String): Boolean = {

    val map = Map(")" -> "(", "}" -> "{", "]" -> "[")
    val strings = Stack[Char]()

    for(i<-0 until s.length){

    }
  }
}
